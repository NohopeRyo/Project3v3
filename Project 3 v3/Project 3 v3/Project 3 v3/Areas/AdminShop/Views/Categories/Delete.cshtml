@model Project_3_v3.Models.Category

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/AdminShop/Views/Shared/_LayoutAdmin.cshtml";
}
<div ng-controller="Category" class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Quản lý/Quản lý thể loại nhạc/</span>Xoá thể loại nhạc</h4>
    <input id="id" hidden value="@Model.CateID" type="text" class="form-control" placeholder="Ma" aria-label="Username" aria-describedby="basic-addon1">
    <div class="card">
        <h5 class="card-header">Delete</h5>
        <div class="card-body">
            <div class="mb-3 col-12 mb-0">
                <div class="alert alert-danger">
                    <h6 class="alert-heading fw-bold mb-1">Bạn có chắc chắn muốn xoá thể loại nhạc này?</h6>
                    <p class="mb-0">Một khi đã xoá thể loại này,sẽ không có chuyện comeback đâu.Hãy thận trọng quyết định này</p>
                </div>
            </div>
            <form id="formAccountDeactivation" onsubmit="return false">
                <div class="form-check mb-3">
                    <input class="form-check-input"
                           type="checkbox"
                           name="accountActivation"
                           id="accountActivation" />
                    <label class="form-check-label" for="accountActivation">Đồng ý với quyết định này</label>
                </div>
                <input ng-click="Delete()" value="Delete" class="btn btn-danger deactivate-account" /> |
                @Html.ActionLink("Back to List", "Index")
            </form>
        </div>
    </div>
</div>
<script src="~/Scripts/angular.min.js"></script>
<script>
    var app = angular.module('Rogost', []);
    app.controller('Category', function ($scope, $http, $window) {
        $scope.category = {}
        $scope.Delete = Delete;
        function Delete() {
            var config = {
                params: {
                    id: $("#id").val(),
                }
            }
            $http.post("/AdminShop/Categories/DeleteObj?id=" + $("#id").val(), null).then(function (result) {
                $window.location.href = '/AdminShop/Categories';
            }, function (error) {
                console.log(config)
                alert("404 Error")
            });
        }
    });
</script>


